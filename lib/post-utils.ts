// Notice the name is more descriptive of what it does.
export async function getFypPostIds(): Promise<number[]> {
  // We'll have to make the logic for choosing posts here.
  // For now, just return a hardcoded list.
  return [9, 10, 11];
}